# Repository Guidelines & Workflow Rules

## Git Commit & Push Workflow
- **Always commit and push to Git every time changes are made**: Whenever any modification, fix, feature, or asset update is completed and verified, immediately stage all relevant changes (`git add`), commit with a descriptive conventional commit message (`git commit -m "..."`), and push to `origin main` (`git push origin main`).
